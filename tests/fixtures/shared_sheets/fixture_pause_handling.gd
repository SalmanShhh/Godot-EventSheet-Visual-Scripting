## A shared event sheet wired as a helper: the including script keeps one and forwards to it.
## @ace_shared_sheet(helper)
class_name FixturePauseHandling
extends RefCounted


func on_tick(host: Node, delta: float) -> void:
	pass
