class_name OpenedScriptStructureBase
extends Node2D
## The base script the structure fixture extends, so its Include bar has a real project file to
## name and open. Nothing here runs - it exists to be inherited from.


func _ready() -> void:
	pass


func take_damage(amount: int) -> void:
	pass
