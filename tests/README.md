# EventForge tests

Run all tests headlessly:

```bash
godot --headless --script tests/run_tests.gd
```

This currently validates the Phase 1 compiler by comparing generated output against the demo golden file.
